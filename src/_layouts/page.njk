---
layout: base.njk
---
{% include 'sitenav.njk' %}

<main>
  <div class="narrative">
    <header class="page-header">
      <h1 class="page-header__title">{{ title }}</h1>
      {% if description %}<p class="page-header__desc">{{ description }}</p>{% endif %}
    </header>
    {{ content | safe }}
  </div>
</main>
